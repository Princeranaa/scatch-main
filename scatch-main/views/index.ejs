<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch-main</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950">
    <%- include('./partials/header') %>

    <% if(error.length > 0) { %>
        <div class="absolute top-5 left-1/2 -translate-x-1/2 p-3 rounded-md bg-red-500">
            <span class="inline-block text-white">
                <%= error %>
            </span>
        </div>
    <% } %>

    <div class="flex flex-col lg:flex-row items-center lg:items-stretch w-full min-h-screen px-5 lg:px-20">
        <!-- Left Side -->
        <div class="flex-1 flex items-center justify-center py-10 lg:py-0">
            <div class="w-full max-w-md px-5">
                <h3 class="text-4xl text-white text-center lg:text-left">Welcome to 
                    <span class="text-blue-400 font-semibold">Scatch</span>
                </h3>
                <h4 class="text-2xl mb-5 text-white text-center lg:text-left">Create your account</h4>
                <form autocomplete="off" action="/users/register" method="post">
                    <input
                        class="bg-transparent text-zinc-200 block w-full px-3 py-2 border border-zinc-200 rounded-md mb-3"
                        type="text" placeholder="Full Name" name="fullname">
                    <input
                        class="bg-transparent text-zinc-200 block w-full px-3 py-2 border border-zinc-200 rounded-md mb-3"
                        type="email" placeholder="Email" name="email">
                    <input
                        class="bg-transparent text-zinc-200 block w-full px-3 py-2 border border-zinc-200 rounded-md mb-3"
                        type="password" placeholder="Password" name="password">
                    <input
                        class="px-5 py-3 mt-2 bg-blue-500 text-white rounded-full w-full cursor-pointer"
                        type="submit" value="Create My Account">
                </form>
            </div>
        </div>

        <!-- Right Side -->
        <div class="flex-1 flex items-center justify-center py-10 lg:py-0">
            <div class="w-full max-w-md px-5">
                <h4 class="text-2xl capitalize mb-5 text-white text-center lg:text-left">Login to your account</h4>
                <form autocomplete="off" action="/users/login" method="post">
                    <input
                        class="bg-transparent text-zinc-200 block w-full px-3 py-2 border border-zinc-200 rounded-md mb-3"
                        type="email" placeholder="Email" name="email">
                    <input
                        class="bg-transparent text-zinc-200 block w-full px-3 py-2 border border-zinc-200 rounded-md mb-3"
                        type="password" placeholder="Password" name="password">
                    <input
                        class="px-5 py-3 mt-2 bg-blue-500 text-white rounded-full w-full cursor-pointer"
                        type="submit" value="Login">
                </form>
            </div>
        </div>
    </div>

    <%- include('./partials/footer') %>
</body>
</html>
